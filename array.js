

var age = [34, 54, 32, 65, 3254];
var friendsName = ['rakib', 'shakil', 'tanim',];
console.log(age.length);

// use [0] position no. to access array elements 
// use (.length) to know how many elements has in the array.



var serials = [34, 5, 532, 5, 1, 65, 432, 34];
var number = serials[5];
console.log(number);

serials[3] = 99;
serials[6] = 21;

// use index.Of to find the index No. of an element from an array 

var index = serials.indexOf(1);
console.log(index);